upvalue=function(x)
{
    b=10;
    add=function(a)
    {
        return a+b;
    };

    return add(x);
};

println(upvalue(100));

fn = function(a,b)
{
    a = 10;
    {
        b = 10 + a;
        fn2 = function(x)
        {
            return x+b;
        };
        return fn2;
    }
    
};

println(fn(10,20)(30));