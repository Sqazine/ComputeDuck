function fib(x)
{
    if(x==0) 
        return 0;
    else if(x==1) 
        return 1;
    else 
        return fib(x-1)+fib(x-2);
}
var start=clock();
var a=fib(35);
println(a);# 9227465
var end=clock();
println(end-start);
