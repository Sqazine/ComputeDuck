fib=function(x,pre,cur)
{
    if(x==0)
        return pre;
    else if(x==1)
        return cur;
    else 
        return fib(x-1,cur,pre+cur);
};
start=clock();
a=fib(35,0,1);
println(a);# 9227465
end=clock();
println(end-start);
